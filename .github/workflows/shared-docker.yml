# Reusable workflow for building and pushing Docker images
name: shared-docker

on:
  workflow_call: # 👈 makes the workflow reusable and allows it to be called from other workflows
    inputs:
      image-tag:
        description: 'Docker image tag'
        required: true
        type: string
      push-latest:
        description: 'Whether to also tag the image as latest'
        required: false
        type: string
        default: 'false'
    # 👇 Secrets are scoped to the workflow that owns them and are not automatically inherited by reusable workflows to have full visibility and control over what sensitive data is being passed around (GitHub Actions security design decision). Thus it must be explicitly passed from the caller workflow.
    secrets:
      DOCKERHUB_TOKEN:
        description: 'Docker personal access token'
        required: true

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Build and push Docker image
        uses: ./.github/actions/build-push-docker
        with:
          docker-username: ${{ vars.DOCKERHUB_USERNAME }}
          docker-repository: ${{ vars.DOCKERHUB_REPO }}
          docker-token: ${{ secrets.DOCKERHUB_TOKEN }}
          image-tag: ${{ inputs.image-tag }}
          push-latest: ${{ inputs.push-latest }}
